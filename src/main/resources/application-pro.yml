spring:
  datasource:
    username: root
    password: 2816077466
    url: jdbc:mysql://localhost:3306/fxr_blog?serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

logging:
  level:
    root: warn
    com.fxr.myblog: info
  file:
    name: log/myblog-pro.log

server:
  port: 3010